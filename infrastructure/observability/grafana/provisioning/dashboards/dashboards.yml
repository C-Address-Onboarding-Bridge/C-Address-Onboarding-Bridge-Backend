apiVersion: 1

providers:
  - name: bridge-api
    orgId: 1
    folder: Bridge API
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
