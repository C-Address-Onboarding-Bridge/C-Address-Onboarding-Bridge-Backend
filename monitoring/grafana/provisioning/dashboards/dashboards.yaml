apiVersion: 1

providers:
  - name: "C-Address Bridge"
    orgId: 1
    folder: "C-Address Bridge"
    folderUid: "cab-folder"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
